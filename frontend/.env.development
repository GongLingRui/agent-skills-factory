# 本地开发：与后端 DEV_WIDGET_AUTH_BYPASS=true 配合，打开 /apps 或 /admin 即自动建立会话（无需 portal JWT）
VITE_DEV_WIDGET_AUTH_BYPASS=true
# POST /auth/dev/session 使用的默认 agent_id（需在后端存在且 active）
VITE_DEV_DEFAULT_AGENT_ID=demo-agent

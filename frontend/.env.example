# 复制为 .env / .env.local 可覆盖。开发默认见 .env.development
# VITE_API_BASE=/api/v1
# 与后端 DEV_WIDGET_AUTH_BYPASS 同时开启时，打开 /apps 或 /admin 会自动建立会话
# VITE_DEV_WIDGET_AUTH_BYPASS=true
# VITE_DEV_DEFAULT_AGENT_ID=demo-agent
